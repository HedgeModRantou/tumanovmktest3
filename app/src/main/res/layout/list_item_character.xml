<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp">

    <!--
        Иконка пути: ImageView, в который программно (CharacterAdapter)
        устанавливается GradientDrawable в форме круга нужного цвета.
        Это и есть обязательная "картинка" элемента списка.
    -->
    <ImageView
        android:id="@+id/pathIcon"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_margin="8dp"
        android:layout_gravity="center_vertical"
        android:scaleType="fitCenter"
        android:contentDescription="Иконка пути" />

    <!-- Текстовая информация о персонаже -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="center_vertical"
        android:paddingStart="12dp">

        <!-- Имя персонажа -->
        <TextView
            android:id="@+id/characterName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:textStyle="bold" />

        <!-- Название пути -->
        <TextView
            android:id="@+id/characterPath"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:paddingTop="4dp" />

    </LinearLayout>

</LinearLayout>
